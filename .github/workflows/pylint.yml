name: Pylint

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: prefix-dev/setup-pixi@v0.8.0
      with:
        pixi-version: v0.39.0
        cache: false
    - run: pixi run lint